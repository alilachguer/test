{% block body %}
    <h1>get.html.twig</h1>
    <pre>{{ dump(results['relationsEntrantes']|length) }}</pre>
    <ul>
        {% for result in results %}
                {#<li id="{{ result.id }}"><a href="#{{ result.id }}" title="{{ result.formatted_name }}">{{ result.name }}</a></li>#}
        {% endfor %}
    </ul>
{% endblock %}
